const express=require("express"),app=express(),PORT=3e3;app.use(express.json());app.get("/api/users",(req,res)=>res.json([{id:1,name:"Alice",role:"admin"},{id:2,name:"Bob",role:"user"}]));app.post("/api/users",(req,res)=>{const{name,role}=req.body;if(!name||!role)return res.status(400).json({error:"name and role required"});res.status(201).json({id:Date.now(),name,role})});app.listen(PORT,()=>console.log("Server running on port "+PORT));
